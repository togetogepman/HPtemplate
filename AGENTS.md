# AGENTS.md

## リポジトリの目的

- 「研究室HPテンプレート」という名称の、研究者個人または研究室向け静的ホームページテンプレートです。
- GitHub Pagesでの公開を想定します。
- HTML、CSS、JavaScriptを直接利用し、ビルド工程を必要としません。
- `index.html`は利用者サイトの正本、`sample.html`は完成見本と回帰試験用です。

## 優先順位

1. 掲載情報の正確性
2. 既存データの保全
3. 表示と操作の安定性
4. 非エンジニアによる編集しやすさ
5. デザイン
6. 高度な機能

## 事実性と安全性

- 入力資料にない所属、肩書、論文、受賞、研究費、共同研究先、経歴等を創作しないでください。
- 不足情報は明示的なプレースホルダーとして残すか、利用者へ不足を報告してください。
- 著者、誌名、巻号、年、DOI等を組み合わせた実在しそうな偽論文を生成しないでください。
- 論文、受賞、経歴、所属、職位は入力資料と照合してください。
- 学生氏名、顔写真、進路、個人メール、未公開研究等は、利用者が掲載許可と公開範囲を確認する必要があります。
- APIキー、認証情報、script ID、スプレッドシートID等をHTML、文書、fixture、ログ、commit messageへ記録しないでください。

## 編集ルール

- 作業前に`DESIGN.md`の現行仕様と「デザイン変更指示」を読んでください。
- デザイン変更指示がすべて「なし」の場合は、現行デザインを維持してください。
- 横断的な色、フォント、余白、角丸、罫線、アニメーション時間は、まず`assets/css/theme.css`で対応できるか確認してください。
- `theme.css`で対応できる変更を、無関係なHTML変更や大規模な`style.css`書換えで実現しないでください。
- `style.css`はレイアウトとコンポーネント実装、`theme.css`は変更頻度の高いデザイン値を担当します。
- `sample.html`は完成見本・件数境界・長文・空データの確認に使います。
- `index.html`は利用者の実サイトとして編集する正本です。サンプル人物やサンプル業績を混入させないでください。
- CSS、JavaScript、画像資産をページ別に複製しないでください。
- セクション、ボタン、ロゴ等を削除しても無関係なJavaScriptを停止させないでください。
- 外部依存を安易に追加せず、`@latest`を使用しないでください。
- SimpleBarを再導入しないでください。
- フレームワーク、ビルド工程、常設npm依存を勝手に導入しないでください。
- CMS、DB、API、認証、問い合わせ送信、決済等を勝手に追加しないでください。
- 今回の目的に関係しないリファクタリングや一括整形を行わないでください。

## 初回カスタマイズ

- 初回カスタマイズでは`input/laboratory-input.xlsx`を利用者入力の正本とし、`00_チェック`と各シートの実入力を確認してから`index.html`へ反映してください。
- シート名、列見出し、表示設定、セクション名、判定、データ内容を意味ベースで読み、空の予備行を除外しつつ、判定式や色がない追加行も確認してください。
- 「表示しない」セクションや論文分類を削除する場合は、ナビゲーション、タブ、Swiper、もっと見る、対象DOMを前提にしたJavaScript等も調査し、残る機能を壊さないでください。
- `sample.html`は初回入力元にせず、Excelの未完了なサンプル情報を`index.html`へ反映しないでください。

## 帰属表示

- フッターの株式会社アカデメイアの帰属表示とサービスリンクは、無料利用条件の一部です。
- 利用者が有償の帰属表示削除許諾を取得済みであることを明示しない限り、削除しないでください。
- 不明な場合は削除せず、利用者へ報告してください。
- `display:none`、`visibility:hidden`、背景同色、極端な縮小、画面外配置等で実質的に見えなくしないでください。
- リンク先を`https://www.academeia15.co.jp/web-template`以外へ変更しないでください。
- `index.html`と`sample.html`の両方で維持してください。
- 公開前検査ではDOM内の存在、画面上の可視性、クリック可能性、正式URLをPC/SPで確認してください。
- Codex自身が有償許諾の契約成立や有効性を保証するものではありません。

## 必須検証

- `index.html`と`sample.html`のHTML構造
- `theme.css`、`style.css`、その他CSSの構文
- `script.js`と`library.js`のJavaScript構文
- ブラウザのconsole error / warning
- PC表示とスマートフォン表示、横方向オーバーフロー
- モバイルメニュー、ナビゲーション、Swiperタブ
- NEWS 0・1・3・4件以上
- メンバー0・1・2・3グループ以上
- 論文0・1・6・7件以上、タブ0・1・複数
- Contactの通常コピーと可能な範囲でfallback
- ローカル画像、CSS、JavaScriptの404
- キーボード操作とフォーカス表示
- 両ページの帰属表示と正式サービスURL
- プレースホルダー、仮リンク、危険なサンプルデータ、認証情報の残存
- 自動検証できない事実・掲載許可・権利関係は、利用者確認が必要として報告

## Git運用

- 作業前に現在のディレクトリ、branch、HEAD、working tree、remoteとの差分を確認してください。
- remoteを利用する作業では、最初に`git fetch`してください。
- 既存の未コミット変更を勝手に変更、破棄、stash、reset、checkout、rebase、commitしないでください。
- 今回の変更と既存変更を分離できない場合は作業を停止し、理由を報告してください。
- 実装後に関連する非破壊的テストと静的検査を実施してください。
- テスト成功後、今回の変更だけをcommitし、現在の作業branchへpushしてください。
- 新規branch、PR、`main`へのmerge、tag作成、branch削除は、明示的な依頼がある場合だけ行ってください。
- 最終報告では、結論、変更内容、テスト、変更ファイル、commit、push先、working treeとremote同期、残課題を区別してください。
