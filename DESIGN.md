# 研究室HPテンプレート デザイン仕様

## 1. この文書の役割

この文書は、現在のデザインを文章で記録した設計正本です。Codexはデザイン変更前にこの文書と`assets/css/theme.css`、`assets/css/style.css`を照合してください。

この文書を書き換えただけではブラウザ表示は変わりません。利用者は末尾の「デザイン変更指示」を編集した後、`prompts/APPLY_DESIGN.md`をCodexへ渡して実装と検証を依頼します。

## 2. 現行デザインの基本コンセプト

- 研究内容を読みやすく見せる、白と淡いグレーを基調とした1ページ構成
- 青を見出し、選択状態、Contactに限定して使う
- PCでは左固定ナビゲーション、スマートフォンでは上部固定ヘッダーとドロワーメニュー
- 大きな見出しと十分な余白でセクションを区切る
- 装飾より、長い研究テーマ、経歴、論文情報の読みやすさを優先する
- `index.html`と`sample.html`で情報構造、CSS、JavaScriptを共有する

## 3. 現行デザイントークン

変更頻度の高い値は`assets/css/theme.css`の`:root`にあります。

| 用途 | CSS変数 | 現行値 |
|---|---|---|
| メインカラー | `--color-primary` | `#2269d3` |
| メインカラー濃色 | `--color-primary-dark` | `#2a5eab` |
| 本文色 | `--color-text` | `#302f37` |
| 白い面 | `--color-surface` | `#fff` |
| 選択面 | `--color-surface-selected` | `#f3f4fb` |
| 交互セクション背景 | `--color-section-alternate` | `#fbfcff` |
| リンク色 | `--color-link` | `#1a55ad` |
| 補助文字色 | `--color-muted` | `#a0a0a0` |
| 罫線色 | `--color-border` | `#d4dbe1` |
| タブ背景 | `--color-tab-surface` | `#f9f9f9` |
| フォーカス色 | `--color-focus` | `#2269d3` |
| 基本フォント | `--font-family-base` | `"Noto Sans JP", sans-serif` |
| 基本文字サイズ | `--font-size-base` | `16px` |
| カード角丸 | `--radius-card` | `6px` |
| パネル角丸 | `--radius-panel` | `10px` |
| 小要素角丸 | `--radius-small` | `4px` |
| ピル型角丸 | `--radius-pill` | `100vh` |
| 基本アニメーション | `--duration-base` | `.3s` |
| 高速アニメーション | `--duration-fast` | `.2s` |
| 低速アニメーション | `--duration-slow` | `.4s` |
| メニューアニメーション | `--duration-menu` | `.55s` |

`--content-max-width`は`none`です。現行デザインは全体の最大幅を固定せず、PCの左右余白とナビ幅で読み幅を作っています。

## 4. タイポグラフィ

- Google FontsからNoto Sans JPの400、500、600、700を読み込む
- `html`の基準は16px、本文色は`--color-text`
- セクション見出し`h2`はスマートフォン32px、PC45px、font-weight 500
- `h2`下の青線はスマートフォン48×6px、PC60×9px
- `h3`はスマートフォン20px、PC22px、font-weight 700
- `h4`はスマートフォン18px、PC22px、font-weight 700
- 本文は主に13～16px、行間は1.4～2.0
- NEWS、研究テーマ、論文等の長文には`overflow-wrap: anywhere`を適用

文字サイズはコンポーネントごとの読みやすさに関係するため、現時点では`style.css`に残しています。

## 5. レイアウトとコンテンツ幅

- 全体はビルド工程のない静的1ページ
- PCの本文は左ナビ250pxを避けて表示
- スマートフォンのセクション余白は上下50px、左右30px
- PCのセクション余白は上下80px、左右130px
- 最初の研究テーマだけ上余白をスマートフォン30px、PC90pxに調整
- 1450px以上では研究テーマ本文を44.444vw、メンバーとプロフィール本文を40.972vwへ拡張
- 研究テーマ本文、メンバー、プロフィールの通常PC幅は589px
- セクションは偶数番目を`--color-section-alternate`にし、追加の注意書き等で背景順がずれないよう`section:nth-of-type(even)`を使う

## 6. ヘッダーとナビゲーション

- ヘッダーは画面上部固定、白背景、下罫線1px
- 実測のヘッダー高はスマートフォン67px、PC83px
- スマートフォンの本文オフセットは既存仕様の66px
- PCでは幅250pxの左固定ナビ、本文とフッターも250px右へずらす
- スマートフォンでは幅300pxの右ドロワーを表示
- ドロワーは`.55s`のcubic-bezierで移動し、背景オーバーレイを使う
- ナビ項目は各`section`の`id`と`data-title`からJavaScriptで生成
- 現在位置は左側の青い3pxバーと淡い選択面で表す
- 論文セクションはアコーディオン式の子ナビを持つ

## 7. セクション共通仕様

- `h2`、青い下線、本文の順で構成
- 白と淡い背景を交互に使用
- 区切り線は`--color-border`
- 長文と英語表記は強制改行せず、必要な位置で自然に折り返す
- セクションを削除してもナビゲーションと他機能が停止しないこと
- 「もっと見る」対象は`hidden`属性と`.is-hidden`を併用し、フォーカス順からも外す

## 8. NEWS

- 日付は補助色、タイトルは本文色と太字
- 各項目の下に破線を表示
- スマートフォンは初期3件、4件目以降を「もっと見る」で3件ずつ表示
- 外部リンクがある項目だけ`.is-link`を付け、青いリンクと外部リンクアイコンを表示
- PCのhoverではリンクを50%不透明にする

## 9. 研究テーマ

- 見出し前にCSSカウンターで`01`、`02`等を付ける
- スマートフォンは縦積み、PCは見出し260pxと本文589pxの横並び
- 画像はスマートフォンで横幅100%、PCでは最大高200pxで縦横比を保持
- 各項目の下に罫線を置き、最後の項目だけ罫線を外す

## 10. 研究室のモットー

- 白背景のカード
- 罫線1px、角丸6px、内側余白30px
- 見出しと本文を縦方向に並べる

## 11. メンバー

- 職位・学年グループ単位で上罫線を表示
- PCでは見出し260px、氏名一覧589pxの横並び
- 初期2グループ、3グループ目以降を「もっと見る」で表示
- 氏名、進路、所属先の公開許可はデザインとは別に利用者が確認する

## 12. プロフィール

- スマートフォンは画像と氏名を横並び、経歴等をその下に表示
- PCはプロフィール概要を右、経歴・受賞・学会を左にした横構成
- プロフィール画像枠は3:4、最大幅240px、未設定時は明示的なプレースホルダー
- 年は補助色で最小幅70px、本文と横並び
- 外部リンクはURLがある場合だけ表示する

## 13. 論文・研究発表

- Swiper 11.0.5を使用し、タブと内容スライドを連動
- タブは淡いグレー面、選択中は青い文字・下線・選択面
- PCはパネル全体を罫線1px、角丸10pxで囲む
- 内容は最大高600pxでネイティブ縦スクロール
- 初期6件、7件目以降を「もっと見る」で6件ずつ表示
- 0件タブでは入力案内を中央表示

## 14. Contact

- セクション背景は濃い青`--color-primary-dark`
- 見出しと下線は白
- 内容は白いカード、角丸6px、スマートフォン30px内側余白
- 問い合わせボタンは濃い青、白文字、小角丸
- メールコピー成功メッセージは`.2s`で表示
- 地図未設定時は破線枠と選択面色のプレースホルダーを表示

## 15. フッターと帰属表示

- `Created with`、アカデメイアロゴ、copyrightを中央配置
- 補助文字は12px相当と`--color-muted`
- PCでは左ナビ250pxを避けて配置
- アカデメイアのロゴと`https://www.academeia15.co.jp/web-template`へのリンクは無料利用条件の一部
- 帰属表示を非表示、背景同色、極端な縮小、別URL化しない

## 16. 画像の扱い

- `<picture>`と`img`を使い、原則として元の縦横比を維持
- メインビジュアルは横長、研究テーマ画像は内容に応じて可変
- OGPは`assets/images/common/ogp.jpg`
- faviconはICOとSVG、Apple Touch Iconを使用
- 写真、人物、大学ロゴを差し替える前に掲載権限を確認
- `sample.html`の画像は表示確認用であり、利用者の実サイトでは適切な画像とaltへ差し替える

## 17. レスポンシブ仕様

- スマートフォン: `max-width: 768px`
- PC: `min-width: 769px`
- 大画面補正: `min-width: 1450px`
- ブレークポイント変更時にページをreloadしない
- PC/SP切替時にメニュー、ナビ、Swiper、展開状態が破綻しないこと
- 横方向スクロールを発生させないこと

## 18. アニメーションとインタラクション

- 表示追加: `.3s`のfade-in
- 一般hover・状態変化: `.2s`～`.3s`
- リンクhover: `.4s`で入り、`.2s`で戻る
- ドロワーメニュー: `.55s`
- ボタンactive: `scale(.97)`
- `html`は`scroll-behavior: smooth`
- 動きを増やす場合も、情報取得を妨げる高度なアニメーションは追加しない

## 19. アクセシビリティ

- リンクは移動、ボタンは操作に使い分ける
- キーボードフォーカスは青3pxのoutlineと3pxのoffsetで可視化
- モバイルメニューは`aria-expanded`とaria-labelを更新
- コピー結果は`role="status"`と`aria-live="polite"`で通知
- 非表示項目は表示だけでなくアクセシビリティツリーとフォーカス順から外す
- 画像には用途を説明するaltを付ける
- 色だけに依存せず、下線、罫線、文字等でも状態を示す

## 20. 変更時に維持すべき要件

- `index.html`は利用開始用正本、`sample.html`は完成見本・回帰試験用
- HTMLの情報構造とJavaScriptの欠落耐性を維持
- NEWS 3件、メンバー2グループ、論文6件の境界を維持
- PC/SP、キーボード操作、フォーカス表示を維持
- 色、フォント、余白等の横断変更はまず`theme.css`で検討
- 1コンポーネント固有の構造・表示は`style.css`に残す
- CSS・JavaScriptをページ別に複製しない
- アカデメイアの帰属表示と正式リンクを両ページで維持
- 別デザインで情報構造が大きく異なる場合だけ、必要なHTML変更を検討する
- この文書は自動設定ファイルではなく、Codexが実装意図を理解するための設計正本

## 21. デザイン変更指示

変更希望がない場合は「なし」と記載してください。この領域を編集した後、`prompts/APPLY_DESIGN.md`をCodexへ渡してください。

### 目指す印象

なし

### 色

なし

### フォント

なし

### 余白・密度

なし

### セクション別の変更

なし

### 変更しないもの

- 情報構造
- スマートフォン対応
- キーボード操作とフォーカス表示
- NEWS、メンバー、論文の件数境界
- アカデメイアの帰属表示と正式リンク
