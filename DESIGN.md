# 研究室HPテンプレート デザイン仕様（DADS試作版）

## 1. この文書の役割

この文書は、`dads-prototype` branchで実装した現在のデザインを文章で記録する設計正本です。Codexはデザイン変更前に、この文書と`assets/css/theme.css`、`assets/css/style.css`、`assets/js/script.js`を照合してください。

この試作はデジタル庁デザインシステム（DADS）の設計思想を研究室サイト向けに再解釈したものであり、デジタル庁の公式テンプレートやデジタル庁サイトの複製ではありません。デジタル庁のロゴ、政府機関を示す表現、公式性を示す表示は使用しません。

## 2. 基本コンセプト

- 研究室を初めて訪れる学生、研究者、共同研究候補者が必要な情報を順番に理解できる1ページ構成
- 装飾より、明確な見出し階層、読みやすい本文、意味のある余白を優先
- 白と淡いグレーを基調とし、プライマリーカラーは濃い青1色に集約
- リンク、ボタン、ラベル、カードの役割を見た目とHTMLの両方で区別
- PCでは上部の水平ナビゲーション、モバイルでは開閉式メニュー
- JavaScriptが無効でも、ナビゲーションと全コンテンツを閲覧可能
- Swiper、SimpleBar、UIフレームワーク、外部Webフォントを使用しない
- `index.html`を利用開始用の正本、`sample.html`を完成見本・件数境界・長文・空データの回帰試験用とする

## 3. 参照したDADSの設計原則

実装時点で確認したDADSはv2.16.0です。主に次を採用しています。

- カラー: テキスト4.5:1以上、主要な非テキスト境界3:1以上を目安とし、前景色と背景色を組み合わせて定義
- タイポグラフィ: サンセリフ、日本語システムフォント、見出し・本文ごとの一貫した行高
- 余白: 8 CSS pxを基準にした段階的な余白スケール
- レイアウト: 最大幅を持つ中央1カラムを基本とし、内容に応じて2列・3列へ分割
- リンク: 青色と下線を組み合わせ、リンク先の目的が分かるラベルを使用
- ボタン: 48pxの高さを基本とし、44×44 CSS px以上の操作領域を確保
- リソースリスト: NEWSや業績を罫線で区切った走査しやすい一覧として表示
- 説明リスト: プロフィールの項目名と内容を`dl`、`dt`、`dd`で関連付け
- アクセシビリティ: 明確なフォーカス、キーボード操作、スキップリンク、状態のARIA通知

参照先は`docs/dads-prototype.md`に記録します。

## 4. デザイントークン

変更頻度の高い値は`assets/css/theme.css`の`:root`で管理します。

| 用途 | CSS変数 | 現行値 |
|---|---|---|
| プライマリー | `--color-primary` | `#0017c1` |
| hover | `--color-primary-hover` | `#000082` |
| active | `--color-primary-active` | `#000060` |
| 本文 | `--color-text` | `#1a1a1a` |
| 補助文字 | `--color-text-subtle` | `#4d4d4d` |
| ページ背景 | `--color-background` | `#f7f7f8` |
| 白い面 | `--color-surface` | `#ffffff` |
| 淡い強調面 | `--color-surface-tint` | `#eef2ff` |
| 主要罫線 | `--color-border` | `#767676` |
| 補助罫線 | `--color-border-light` | `#d8d8d8` |
| リンク | `--color-link` | `#0017c1` |
| 訪問済みリンク | `--color-link-visited` | `#86198f` |
| フォーカス背景 | `--color-focus` | `#ffd43d` |
| フォーカス枠 | `--color-focus-outline` | `#1a1a1a` |
| 余白 | `--space-1`〜`--space-7` | 8、16、24、32、48、64、96px相当 |
| 本文サイズ | `--font-size-body` | `1rem` |
| 見出し1 | `--font-size-heading-1` | `clamp(2rem, 5vw, 3.75rem)` |
| 見出し2 | `--font-size-heading-2` | `clamp(1.75rem, 3.5vw, 2.75rem)` |
| 最大コンテンツ幅 | `--content-width` | `75rem` |
| 読み物最大幅 | `--reading-width` | `56rem` |

研究室ごとの色変更は、まず`--color-primary`と関連するhover・active値を同じ色相の十分なコントラストを持つ階調へ変更します。

## 5. タイポグラフィ

- 外部フォント配信には依存しない
- 基本書体は`Noto Sans JP`が端末にあれば使用し、その後に`Hiragino Kaku Gothic ProN`、`Yu Gothic`、Meiryo、sans-serifを指定
- 本文は16px相当、行高1.75
- `h1`は32〜60px相当、`h2`は28〜44px相当を`clamp()`で連続的に調整
- 見出しの行高は1.4、本文は1.75
- 英語名称や小見出しは補助情報として扱い、主見出しより小さく表示
- 長い日本語、英語、DOI、URL相当の文字列には`overflow-wrap: anywhere`を適用

## 6. レイアウト

- ページ全体はビルド工程のない静的1ページ
- コンテンツは最大1200px相当の中央コンテナに配置
- NEWS、メンバー等の走査性が重要な一覧は最大896px相当の読み物幅を使用
- 主要セクションの上下余白は48〜96px相当を画面幅に応じて調整
- モバイルの左右余白は16px
- 研究カードはPCで3列、中間幅で2列、モバイルで1列
- ヒーロー、プロフィール、ContactはPCで2列、モバイルで1列
- 固定高のカードや本文領域を作らず、内容量に応じて伸長

## 7. ヘッダーとナビゲーション

- 白背景、下罫線、画面上部にsticky表示
- 研究室名と英語名称を常に表示
- PCは主要7セクションへの水平ナビゲーション
- 768px未満はメニューボタンと開閉式ナビゲーション
- メニューボタンは`aria-controls`、`aria-expanded`、状態に応じた`aria-label`を持つ
- メニューを開いた直後は先頭リンクへフォーカスを移す
- Escapeキーで閉じ、メニューボタンへフォーカスを戻す
- メニュー内リンクの選択後はメニューを閉じる
- JavaScriptが無効の場合はモバイルでもナビゲーションを通常表示する
- ページ先頭に「本文へ移動」スキップリンクを設ける

## 8. メインビジュアル

- PCは紹介文と画像の2分割、モバイルは画像を先に表示
- 画像上に長い文字を重ねない
- 研究室名、短いメッセージ、説明文、主要導線をテキスト領域へ配置
- 画像はPCで4:3、モバイルで16:10を基本とし、`object-fit: cover`で表示
- 画像の用途を説明するaltと、差し替え時の注意をfigcaptionで示す

## 9. NEWS

- 日付、カテゴリー、タイトルを1項目として罫線で区切る
- タイトルにリンク先がある場合は、タイトル自体をリンクにする
- 同じ「記事を見る」リンクを繰り返さない
- 外部リンクを新しいタブで開く場合は、外部リンク表示と代替テキストを付ける
- すべてのNEWSを初期表示し、件数に依存するJavaScriptを使用しない

## 10. 研究テーマ

- 画像、研究領域、テーマ名、短い説明、目的が分かる導線をカード内に配置
- カード全体をリンクにせず、リンクだけを操作対象にする
- カードの枠線は情報のまとまりを示す用途に限定
- 3列・2列・1列へレスポンシブに切り替える

## 11. 研究室のモットー

- すべてを枠付きカードにせず、上罫線、固定番号、見出し、本文のステートメント形式
- 番号は装飾扱いで`aria-hidden="true"`
- 内容の順序自体が重要な場合は、番号を実テキストとして読み上げ可能に変更する

## 12. 研究室メンバー

- `section`と見出しで所属・課程別にグループ化
- 氏名と所属・学年を同じリスト項目内で関連付ける
- PCはグループ名と一覧を横並び、モバイルは縦並び
- 重要な在籍メンバーは初期状態で非表示にしない
- 件数が多くなりやすいOB／OGのみ`details`で折りたたみ可能
- 氏名、顔写真、進路、所属先の公開許可は利用者が確認する

## 13. プロフィール

- PCは写真と情報を2列、モバイルは縦積み
- 画像未設定時は3:4の明示的なプレースホルダー
- 基本情報は説明リストで氏名、英語表記、職位、所属、専門、学位、外部プロフィール、研究者情報を表す
- 経歴と受賞歴は年と内容を同じリスト項目内に配置
- 所属学会は通常の箇条書き
- 外部URLは確認できた場合だけリンクにする

## 14. 論文・研究発表

- 論文誌、学会発表、外部記事、書籍、その他を通常の見出しで分ける
- Swiper、タブ、横スクロール、固定高の内部スクロールを使用しない
- 業績は年と本文を対応させたリソースリストとして表示
- 0件カテゴリーは入力案内を左罫線付きの空状態として表示
- すべての業績を初期表示し、件数に依存するJavaScriptを使用しない
- 著者、題名、誌名、年、DOI等は入力資料と照合し、実在しそうな偽業績を作らない

## 15. Contact／アクセス

- 濃紺背景上に白い2パネルを置く
- 研究室参加、共同研究、問い合わせ方法を別見出しで区別
- 所在地とアクセスを別見出しで区別
- メールコピーは`button`で実行し、結果を`role="status"`と`aria-live="polite"`で通知
- Clipboard APIが使用できない場合は`document.execCommand("copy")`による限定的なfallbackを試す
- コピー不可の場合は手動コピーを案内する
- 地図未設定時は、埋め込み地図ではなく明示的な案内を表示
- 入力用とサンプルの連絡先・住所は明らかなダミーとする

## 16. フッターと帰属表示

- 研究室名、主要リンク、Created with、アカデメイアロゴ、copyrightを表示
- アカデメイアロゴは白い面を確保し、背景と同化させない
- `https://www.academeia15.co.jp/web-template`を正式リンクとして維持
- 新しいタブで開くことを矢印とスクリーンリーダー向け文言で示す
- 帰属表示を非表示、背景同色、極端な縮小、画面外配置、別URL化しない
- `index.html`と`sample.html`の両方で維持

## 17. インタラクションとアクセシビリティ

- `lang="ja"`、`<!doctype html>`、ランドマーク、順序どおりの見出しを使用
- リンクは移動、ボタンは操作に使用
- すべてのフォーカス可能要素に黒3px枠、3pxオフセット、黄色背景の`:focus-visible`を表示
- ボタンの高さは48px以上、ナビゲーションリンクも十分な操作領域を確保
- リンクは色だけでなく下線でも本文と区別
- 色だけに依存せず、罫線、ラベル、文言でも状態と関係を示す
- 自動再生、自動スクロール、hoverだけで現れる重要情報を使用しない
- `prefers-reduced-motion: reduce`ではスムーズスクロールと実質的なアニメーションを停止
- 装飾的な番号は支援技術から除外し、情報を持つ画像にはaltを設定

## 18. レスポンシブ仕様

- モバイル: 768px未満
- デスクトップ: 768px以上
- カードの中間調整: 1024px以下で3列から2列
- 最低確認幅: 375px、768px、1280px以上
- 横方向オーバーフローを発生させない
- 長い見出し、英語、論文情報を自然に折り返す
- 画像の縦横比を維持し、本文やカードの高さを固定しない
- ブレークポイント変更時にページを再読み込みしない

## 19. ファイル責務

- `index.html`: 利用者が情報を入力する正本。架空の完成データを混入させない
- `sample.html`: 完成見本、NEWS 4件、メンバー3グループ以上、論文7件以上、長文、0件カテゴリーを確認
- `assets/css/theme.css`: 色、余白、フォント、幅、角丸、時間等の横断トークン
- `assets/css/style.css`: レイアウトとコンポーネント実装
- `assets/js/script.js`: モバイルメニューとメールコピーのみ
- `assets/js/library.js`: DADS版からは読み込まない。既存用途の調査可能性を残すため削除しない
- `assets/css/swiper-bundle.min.css`: DADS版からは読み込まない。既存用途の調査可能性を残すため削除しない

## 20. 変更時に維持すべき要件

- 主要10項目と推奨順序
- 入力正本と回帰見本の役割分担
- 静的HTML、CSS、最小限のJavaScript
- JavaScript無効時の主要情報とナビゲーション
- モバイル対応、キーボード操作、明確なフォーカス
- NEWS、メンバー、論文の0件・少数・境界超過時の安定表示
- Contactの通常コピーと可能な範囲のfallback
- ローカル画像、CSS、JavaScriptの参照整合性
- アカデメイアの帰属表示と正式リンク
- 事実性、掲載許可、画像権利、個人情報の利用者確認

## 21. デザイン変更指示

このbranchでは、DADS試作版への変更を実装済みです。次の変更を行う場合は具体的に記載してください。

### 目指す印象

なし

### 色

なし

### フォント

なし

### 余白・密度

なし

### セクション別の変更

なし

### 変更しないもの

- 主要情報項目と意味的なHTML構造
- スマートフォン対応
- JavaScript無効時の閲覧性
- キーボード操作とフォーカス表示
- NEWS、メンバー、論文の件数に依存しない表示
- アカデメイアの帰属表示と正式リンク
